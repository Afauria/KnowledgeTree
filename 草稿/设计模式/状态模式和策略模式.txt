uml类图相同，都能动态改变对象的行为，不同的是状态模式的状态是自动切换的，用户无法改变逻辑，最多指定初始状态，而策略模式是让用户指定更换的策略算法，动态替换算法

策略模式和状态模式区别：

状态模式能够在对象内部改变行为，状态需要持有对象的引用。状态依赖对象，调用对象setState，外部调用状态方法


策略模式由在外部改变行为，策略不需要持有对象的引用。外部调用对象的方法

通过状态模式和策略模式

消除多重if……else：

策略模式将具体行为作为参数代替条件判断，使用面向接口的方式，调用接口方法。


状态模式直接调用具体状态的方法，将条件行为移到状态中。